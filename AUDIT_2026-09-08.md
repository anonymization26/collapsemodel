# Two-Stage 实验审计与修正记录（2026-09-08）

## 1. 结论摘要

截至 2026-09-09，修正版实验可靠支持的结论是：

1. rank-$L$ Gram sketch 的确定性区间在已完成的四编码器扫描中数值有效：2,880 个选中 prefix
   全部覆盖精确分数；但 $L\in\{10,20,50\}$ 的 7,200 个 greedy 步骤没有一个获得排序证书。
2. 受控重叠中，rank-$L$ 相对 rank-only 的平均增益随 $L$ 从 2.09% 增至 4.94%，但 DPP-subspace
   分别达到 4.90% 至 5.68%，现有非等字节比较不支持 rank-$L$ 的实用优势。
3. 自然池严格 held-out Stage-2 未通过基本效用门槛。验证 oracle 的来源监督 adapter 相对
   `frozen_identity` 平均低 0.0670，target-cluster 95% CI 为 [-0.0977, -0.0440]；小 shortlist
   regret 只表示在一个整体有害的 adapter 族内部接近 oracle，不是数据消费收益。
4. 删除 52.38% 候选时，所有方法的严格 top-1 recall 均低于预注册的 90% 门槛。DPP 与 rank-only
   的差异很小，cluster bootstrap 不支持稳定增量。

现有证据不能支持“通用、domain-robust、安全地删除一半候选”或“无标签谱几何保证下游最优”
等主张。自然实验只有一组混合来源池，目标也主要属于通用视觉，尚不能代表广泛跨域稳健性。

## 2. 发现的主要问题

### 2.1 理论与实现不对应

- 数学主线要求直接累加每个来源的 rank-$L$ PSD 因子并计算截断 Gram 有效秩。
- 修正前 `two_stage_summary_only_controlled.py` 先把累计 sketch 再压缩成 rank、核质量和平均
  alignment，然后调用旧四摘要公式；它不是理论中定义的 Truncated-Gram 方法。
- 修正前单池候选使用完整谱 rank/nuclear，累计状态却使用 top-$L$ sketch 谱，处于不一致的
  信息层级。例如旧 CLIP/CIFAR-10 缓存中，完整有效秩约为 129.04，而 top-20 sketch 约为
  12.56；完整核质量约为 102.07，而 sketch 约为 39.51。

### 2.2 overlap AUROC 的负例过于容易

旧 `alignment_duplicate_auroc` 将跨数据集 pair 作为大量负例，只把 alias 与指定 parent 当作
正例。该指标主要检测“是否来自同一数据集”，不是在同分布候选中识别真实 parent。

对旧 CLIP 结果进行只读重算后：

| 样本重叠率 | 旧全局 AUROC | 同数据集 matched AUROC | Parent Top-1 |
|---:|---:|---:|---:|
| 0.00 | 约 0.924 | 约 0.505 | 0.24 |
| 0.10 | 未单独引用 | 约 0.624 | 0.76 |
| 0.25 | 未单独引用 | 约 0.785 | 1.00 |
| 0.50 | 未单独引用 | 约 0.980 | 1.00 |
| 0.75 | 未单独引用 | 1.000 | 1.00 |
| 1.00 | 未单独引用 | 1.000 | 1.00 |

0% 重叠时不存在真实 parent，原 parent 只是构造代码指定的同数据集池。因此修正版将该行的
parent AUROC/AUPRC 设为未定义，只保留 designated-parent Top-1 作为负对照。

### 2.3 精确召回对阈值敏感

旧主实验中 DPP-subspace、`L=10` 的最佳候选召回随实用等价阈值变化如下：

| 验证准确率容差 | Recall |
|---:|---:|
| 0 | 0.8571 |
| 0.0001 | 0.8929 |
| 0.0005 | 0.8929 |
| 0.001 | 0.9286 |
| 0.002 | 0.9643 |
| 0.005 | 0.9643 |

leave-one-candidate sensitivity 下，对应值依次约为 0.8571、0.8895、0.8912、0.9286、
0.9626、0.9660；旧 source-domain 排除实验约为 0.7679、0.7857、0.8214、0.8750、
0.9196、0.9554。旧 DPP `L=10` 的平均测试 regret 为 -0.000710，target-cluster 95% CI
为 [-0.002679, 0.000548]；rank-only 为 -0.000465，95% CI 为 [-0.001674, 0.000278]。
这说明现有数据没有检测到明显效用损失，但不能据此声称精确 top-1 身份被稳定保留。

### 2.4 其他公平性与复现问题

- 来源特征缓存按标签分层抽样，违反严格 Stage-1 label-free 的信息边界。
- 旧自然池包含 11 个医学、4 个数字/字符、5 个通用视觉和 1 个渲染文本来源，但 28 个
  encoder-target oracle 中 24 个来自通用视觉，17 个来自 Tiny-ImageNet，证据高度集中。
- `full_merged_rank` 是逐步精确评分的 greedy，不是组合上界；有效秩目标一般非单调、非次模。
- 经典压力实验复制有效秩最高的来源，系统性放大 rank-only 的弱点。
- 适配恢复键只有 `(source, seed, target)`，没有参数、代码和输入哈希；中断后可能混合两次训练。
- NPU 后端在模块顶层导入，导致普通 CPU 环境无法运行筛选与汇总。
- Scatter 在 `float32` 中形成并使用绝对阈值，会把数值噪声计为真实谱方向。
- Stage-2 只有单层监督 adapter，缺少冻结表示和随机未训练 adapter 参照；尚无 LoRA 或多池混合
  的真实数据消费验证。
- 修正前的 `adapt` 在 shortlist 冻结前为全部候选计算 test accuracy。虽然汇总排序只读取
  validation accuracy，这仍允许人工反复查看 test，不能称为严格 held-out 评估。

## 3. 已完成的代码修正

- 新增可组合 `GramSketch`、直接 rank-$L$ 累积分数、尾部质量界、Theorem 8 区间及逐步排序证书。
- Legacy Collapse 明确使用“精确来源局部标量 + 截断累计 sketch”的历史启发式口径；该消融与
  直接累加 rank-$L$ PSD 因子的主方法分开报告，不再宣称两者具有同一理论保证。
- Stage-1 使用只读取 `H` 的接口；来源缓存默认采用标签无关随机抽样，并对旧分层缓存设置门禁。
- 自然池默认按 `1/sqrt(n_j)` 缩放，实现预先声明的 equal-source 能量权重。
- overlap 同时输出 global-lineage 与 same-dataset matched 指标；0% 重叠不定义 parent ground truth。
- 保留预注册精确 recall，同时新增固定容差曲线和按 adapter seed 配对 bootstrap 的候选置信集。
- 将完整特征方法重命名为 `exact_merged_rank_greedy`，并加入有组合数上限的真实穷举 oracle。
- 经典重复来源默认改为固定种子随机选择；最高秩复制只作为显式压力测试。
- 新增 agglomerative-medoid 与 centroid leverage-score 基线。
- 适配输出绑定脚本、筛选清单、参数和全部输入 SHA-256；按 source/seed 整组原子写入。
- 新增 `frozen_identity` 与 `random_adapter` Stage-2 对照；确定性算法默认开启。
- Stage-2 拆为 validation-only `adapt`、冻结选择 `summarize` 和独立 `heldout` 三步；adapter
  checkpoint 的文件哈希和参数哈希随验证结果保存，test 缓存只在选择 manifest 冻结后打开。
- 适配汇总逐行核查 CV 折数、分区种子、超参数、样本预算、覆盖数、准确率、耗时和 checkpoint；
  不再仅依赖 sidecar 的配置指纹。
- NPU 后端延迟到 `adapt` 阶段导入；CPU 可运行 `screen`、`summarize` 和单元测试。
- Gram/Scatter 在乘法前转为 `float64`，并使用与谱尺度相关的数值秩阈值。
- 修正版缓存和结果使用独立目录，不覆盖历史实验。
- 修正 `collapse_sketch` 的通信计费：除 rank-$L$ 因子外，该启发式还传输边际有效秩和核质量
  两个 `float64` 标量，因此应为 $8(Ld+2)$ bytes/pool。已归档的 v3 rank-sweep manifest 少计
  16 bytes/pool；该勘误不改变选择顺序或质量指标，且不回写已签名结果。

## 4. 仍需重新执行的验证

上述修正改变了抽样、来源权重、算法矩阵和指标定义，所以旧结果不能原样搬入新表。必须完成：

1. 重新提取四编码器的标签无关来源缓存；
2. 重新运行 rank-$L$（至少 $L=10,20,50$）并记录 DPP/Facility/简单摘要的实际通信量；现有结果
   是非等字节比较，严格等字节 Pareto 扫描另列为未完成任务；
3. 报告区间覆盖率、证书率、区间宽度和必要的 full-rank 回退率；
4. 重新运行 repeated-CV Stage-2，并加入 identity/random-adapter 结果；
5. 增加独立医学、字符和通用视觉候选集合及相应目标，而不是只拆分同一个混合集合；
6. 只有 frozen-feature 结果通过后，再执行 LoRA/多池混合的固定 NPU-hours 实验。

论文必须同时报告已完成的修正版负面结果，并把未覆盖的独立域、等字节 Pareto 和端到端训练明确
列为缺口；不得用历史 pilot 替代修正版结果。

## 5. 修正版复算进展

- [x] 四编码器的标签无关来源缓存已按最新 model-state、preprocess、loader、原始文件与 index
  哈希门禁完成重建；受控实验使用的五个来源子集也通过同一门禁。
- [x] `L=10/20/50` 的 v3 受控扫描均完成并通过四编码器完整分片聚合，共 2,880 个配置。
- [x] 所有 2,880 个选中 prefix 的理论区间覆盖精确分数；7,200 个已评估 greedy 步骤的证书率
  为 0，说明当前尾界正确但在 $L\leq50$ 时不能用于实际免回退决策。
- [x] 含 Full-Gram greedy、Random-100 和 prefix 一致率的三构造种子 v3 pilot 已完成并通过
  四编码器聚合门禁，共 288 个配置。$L=20$ 的区间覆盖 288/288 个前缀，但 0/720 步获得证书，
  且 0/288 个有序前缀与 Full-Gram greedy 完全一致；该 pilot 只作精确参照，不替代十种子扫描。
- [x] 精确 pilot 中 rank-$L$/DPP/Full-Gram greedy 相对 rank-only 的平均增益分别为
  3.65%/5.37%/5.81%；rank-$L$ 在 288 个配置中有 88 个低于 rank-only，不能主张经典基线优势。
- [x] v3 自然池 screen、validation-only repeated-CV、冻结 selection manifest 与独立 held-out
  评估均已完成；四编码器各 735 个 source-seed-target 适配结果完整通过聚合门禁。
- [x] Stage-2 对照显示：identity 为 0.83654，random adapter 为 0.77144，验证 oracle 来源适配器
  为 0.76951；oracle 相对 identity 为 -0.06703，相对 random adapter 为 -0.00193。
- [x] `L=10/21` 时 rank-$L$/rank-only/DPP/Full-Gram greedy 的严格召回分别为
  75.00%/85.71%/85.71%/89.29%，均未达到 90% 验收线。

旧 `results/two_stage_multiencoder_controlled_corrected_v2/` 的数值已作废，不得继续写入正文：该轮
使用的边际 scalar 仍来自截断谱，且缺少当前的完整 encoder/index provenance。所有新结论必须来自
`corrected_v3` 清单，并在四编码器、全部计划 seed/overlap/budget/method 行完整时才能聚合。
