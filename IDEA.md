# 两阶段无标签数据池压缩

## 1. 问题定义

假设存在大量候选数据池 \(\mathcal D_1,\ldots,\mathcal D_M\)，但受预算限制，下游训练或
适配过程只能实际使用其中一小部分。候选数据可能没有标签，而且将全部原始样本或冻结特征传输到
中心节点可能并不可行。

现有 Collapse 理论刻画了两个冻结表示池之间的局部 Gram 谱相互作用。因此，它最有价值的
实际用途不是取代下游评估，而是在任务感知选择之前，以较低成本排除谱空间中高度冗余的候选池。

## 2. 核心思路

采用一个两阶段漏斗：

1. **第一阶段：无标签谱预筛选。** 在同一个冻结编码器下计算每个数据池的摘要，使用
   Collapse Summary Predictor 排除在表示空间中边际贡献较小的数据池。
2. **第二阶段：任务感知验证。** 只对预筛选后保留的候选短名单执行小规模但真实的数据消费过程，
   例如在固定计算预算下训练 linear adapter 或 LoRA，并根据验证性能选择最终数据池子集。

两个阶段的职责需要明确区分：

- 第一阶段优化低成本冗余排除和候选召回率。
- 第二阶段处理谱摘要无法识别的信息，包括任务相关性、类别平衡、标签噪声、样本难度和优化效应。

本文拟议贡献是**精确局部 Gram 几何与基于摘要的第一阶段选择器的组合**，而不是声称有效秩
是下游效用的充分统计量。

## 3. 第一阶段：谱预筛选

### 3.1 输入

对每个数据池 \(\mathcal D_i\)，使用相同的编码器、特征维度、预处理流程、中心化规则和
归一化规则，提取冻结特征矩阵 \(H_i\)。

每个数据池输出一个摘要包：

- 样本数量和特征能量；
- 有效秩，或计算有效秩所需的谱熵；
- top-\(k\) 右奇异子空间；
- 可选的分桶谱质量或随机低秩 sketch；
- 用于标识编码器版本和归一化版本的诊断元数据。

top-\(k\) 子空间并不是标量摘要。因此，必须实际测量它的通信成本和潜在隐私泄漏，不能将其
描述为可忽略成本。

### 3.2 边际分数

对于当前已选择集合 \(S\)，估计候选数据池 \(i\) 的边际谱贡献：

\[
\widehat{\Delta}_{\mathrm{spec}}(i\mid S)
=\widehat r_{\mathrm{eff}}(H_{S\cup\{i\}})
-\widehat r_{\mathrm{eff}}(H_S).
\]

第一版实现可以将已选择集合聚合成一个动态摘要，并使用成对的 Collapse Summary Predictor。
后续版本应在相同通信预算下比较标量摘要、分桶谱和低秩 sketch。

### 3.3 选择规则

执行贪心预筛选，直到满足以下任一条件：

- 候选短名单达到固定大小 \(L\)；
- 达到累计样本量或存储预算；
- 所有剩余候选的边际分数均低于阈值 \(\tau\)。

为保留可能有价值的离群候选，应采用以召回率为导向的策略：保留排名前 \(L\) 的候选，以及
所有分数与筛选边界之差不超过容忍阈值的候选。第一阶段的主要评价标准应是它是否错误排除了
最终最优候选，而不仅是其自身的有效秩目标。

### 3.4 置信度与回退机制

当以下一个或多个假设被严重违反时，附加低置信度标记：

- 逐模态能量比 \(\gamma_i\) 的变化较大；
- 逐模态对齐量 \(\alpha_i\) 的变化较大；
- 归一化谱形状距离较大；
- pairing residual 或 cross-coupling residual 较大；
- 编码器、模态或特征归一化规则不兼容。

低置信度候选应直接进入第二阶段，或改用信息更丰富的 sketch 评分。该标记只是经验诊断，
不是经过证明的误差界。

## 4. 第二阶段：任务感知验证

第二阶段在固定的单候选预算下，实际使用候选短名单中各数据池的样本。默认流程如下：

1. 对每个候选使用相同的预训练主干网络初始化；
2. 在固定样本数量、训练步数和随机种子下训练 linear adapter 或 LoRA 模块；
3. 在留出的目标验证集上评估；
4. 根据平均验证性能选择最终数据池子集，同时将计算量和样本使用量纳入目标，或单独报告。

如果选择阶段没有标签，第二阶段可以采用固定的自监督代理损失，并严格将标签保留到最终评估。
论文必须将这种设置与监督验证明确区分。

## 5. 预期部署场景

- 联邦式或多机构数据获取，各节点只输出摘要；
- 在昂贵的预训练或适配之前，从大量公开语料库中选择候选；
- 以任务或数据源为粒度进行持续学习内存管理；
- 检测复制、数据增强或高度重叠的数据发布；
- 减少进入昂贵下游评估的数据混合候选数量。

当候选数据池使用不兼容的编码器、任务需要样本级选择、任务相关性远比冗余更重要，或者完整
特征和训练标签已经可以低成本获取时，该方法不太适用。

## 6. 该设计可以支持的主张

如果实验验证成功，论文可以主张：

- 谱预筛选减少了昂贵的第二阶段评估次数；
- 在高重叠场景中，Collapse 相互作用分数相较仅使用加性秩摘要的方法提高了冗余检测能力；
- 两阶段选择器能够以更低计算成本接近穷举式任务感知选择；
- 置信度诊断可以识别不应信任紧凑摘要的情况。

论文不应主张：

- 四个摘要量能够对合并有效秩给出普适保证；
- 谱多样性能够单独决定下游准确率；
- 在没有实际使用所选数据进行训练的实验时，声称获得端到端训练收益；
- 在没有形式化或经验隐私泄漏分析时，声称具有隐私保护能力。

## 7. 主要研究问题

1. 在保持最终效用不变时，谱预筛选能够节省多少第二阶段计算？
2. 在自然冗余而不只是人工 clone attack 中，alignment 信息是否优于仅使用秩的摘要？
3. 多大的候选短名单能够以较高概率保留任务感知意义下的最优候选？
4. 预测器诊断在什么条件下能够可靠识别摘要模型失效？
5. 传输更丰富的摘要能够获得多少准确率收益，其通信和隐私成本是多少？
