# 两阶段无标签数据池压缩

## 1. 问题定义

假设存在大量候选数据池 \(\mathcal D_1,\ldots,\mathcal D_M\)，但受预算限制，下游训练或
适配过程只能实际使用其中一小部分。候选数据可能没有标签，而且将全部原始样本或冻结特征传输到
中心节点可能并不可行。

现有 Collapse 理论刻画了两个冻结表示池之间的局部 Gram 谱相互作用。因此，它最有价值的
实际用途不是取代下游评估，而是在任务感知选择之前，以较低成本排除谱空间中高度冗余的候选池。

## 2. 核心思路

采用一个两阶段漏斗：

1. **第一阶段：无标签谱预筛选。** 在同一个冻结编码器下计算每个数据池的摘要，使用
   可组合的 rank-$L$ Gram sketch 直接近似候选集合的合并谱，排除在表示空间中边际贡献较小的
   数据池；旧四摘要 Collapse 只作为消融。
2. **第二阶段：任务感知验证。** 对预筛选后保留的候选短名单执行小规模但真实的数据消费过程，
   并根据目标训练集内部的验证性能选择最终数据池子集。当前审计实现使用来源标签训练每个
   bottleneck adapter，再用目标训练集交叉验证选择来源；它不是在目标数据上训练 adapter。

两个阶段的职责需要明确区分：

- 第一阶段优化低成本冗余排除和候选召回率。
- 第二阶段处理谱摘要无法识别的信息，包括任务相关性、类别平衡、标签噪声、样本难度和优化效应。

本文拟议贡献是**精确 Gram 代数、可认证的低秩近似与任务感知第二阶段的组合**，而不是声称
有效秩是下游效用的充分统计量。

## 3. 第一阶段：谱预筛选

### 3.1 输入

对每个数据池 \(\mathcal D_i\)，使用相同的编码器、特征维度、预处理流程、中心化规则和
归一化规则，提取冻结特征矩阵 \(H_i\)。

每个数据池输出一个摘要包：

- 预先声明的来源权重与总平方能量；
- rank-$L$ PSD 因子 $B_i^{(L)}=\Sigma_{i,L}V_{i,L}^{\top}$；
- 尾部核质量、尾部平方质量和尾秩的可认证上界；
- 用于对照的有效秩、top-$L$ 子空间或其他摘要，并分别记录实际字节数；
- 用于标识编码器版本和归一化版本的诊断元数据。

低秩因子和 top-$L$ 子空间都不是标量摘要。因此，必须实际测量通信成本和潜在隐私泄漏，不能
将其描述为可忽略成本，也不能声称 sketch 天然提供隐私。

### 3.2 边际分数

对于当前已选择集合 $S$，拼接已传输因子

\[
\widetilde B_{S\cup\{i\}}=
\begin{bmatrix}B_j^{(L)}\end{bmatrix}_{j\in S\cup\{i\}},
\]

并直接计算候选的截断 Gram 分数：

\[
\widehat F_i
=\log R_G\!\left(\widetilde B_{S\cup\{i\}}^{\top}
\widetilde B_{S\cup\{i\}}\right).
\]

尾部元数据给出真实 Full-Gram 分数区间 $[\widehat F_i-e_i,\widehat F_i+e_i]$。累计状态始终
保留可组合因子，不能再次压成 effective rank、核质量和平均 alignment 后递归调用成对公式。
当前实验记录标量摘要、子空间方法、rank-$L$ sketch 和完整特征参考的实际通信量；严格的等字节
Pareto 扫描仍是待完成实验，现有非等字节结果不能解释为同成本比较。

### 3.3 选择规则

执行贪心预筛选，直到满足以下任一条件：

- 候选短名单达到固定大小 \(L\)；
- 达到累计样本量或存储预算；
- 所有剩余候选的边际分数均低于阈值 \(\tau\)。

为保留可能有价值的离群候选，应采用以召回率为导向的策略：保留排名前 \(L\) 的候选，以及
所有分数与筛选边界之差不超过容忍阈值的候选。第一阶段的主要评价标准应是它是否错误排除了
最终最优候选，而不仅是其自身的有效秩目标。

### 3.4 置信度与回退机制

若某候选区间下界严格高于其余候选上界，则该轮选择被认证为与 exact Full-Gram greedy 一致。
若区间重叠，则只提高竞争候选和已选来源的 sketch rank；仍无法分离时回退到更高 rank 或完整
Gram。编码器、预处理或来源权重不一致属于协议错误，不能用“低置信度”掩盖。

旧四摘要 Collapse 的 pairing/cross-coupling residual 仍可作为局部机制诊断，但它们不替代
rank-$L$ 的确定性尾部区间。

## 4. 第二阶段：任务感知验证

第二阶段在固定的单候选预算下，实际使用候选短名单中各数据池的样本。默认流程如下：

1. 对每个候选使用相同的冻结表示和相同适配器初始化协议；
2. 当前实现用该候选来源的标签，在固定样本数量、训练步数和随机种子下训练来源监督 adapter；
3. 只在目标训练划分内部做交叉验证；此阶段不得打开目标 test 缓存；
4. 保存并哈希每个 `source × adapter seed` 的 adapter 状态；
5. 根据平均验证性能选择最终数据池子集，并将选择明细与输入哈希写入不可变 manifest；
6. 只有 manifest 冻结后，才加载被预先选中的 adapter 状态并对官方 test 划分评估一次。

每次实验同时运行冻结表示 identity 与未训练 random-adapter 基线；两者的 test 指标同样只能在
选择 manifest 冻结后生成。只有适配器相对这两个参照产生稳定增量时，才可把 Stage-2 结果解释为
来源数据消费收益。held-out 结果不得反向改变 shortlist、容差、随机种子或报告方法集合。

为精确估计所有 shortlist 的 regret，当前审计在离线阶段训练了全部候选适配器。因此，结果只衡量
“若适配器已经存在，预筛选会遗漏多少”，并未实测部署时只训练短名单所节省的墙钟时间。部署成本
收益必须在冻结筛选后仅训练入选候选的独立运行中测量。

如果选择阶段没有标签，第二阶段可以采用固定的自监督代理损失，并严格将标签保留到最终评估。
论文必须将这种设置与监督验证明确区分。

## 5. 预期部署场景

- 联邦式或多机构数据获取，各节点只输出摘要；
- 在昂贵的预训练或适配之前，从大量公开语料库中选择候选；
- 以任务或数据源为粒度进行持续学习内存管理；
- 检测复制、数据增强或高度重叠的数据发布；
- 减少进入昂贵下游评估的数据混合候选数量。

当候选数据池使用不兼容的编码器、任务需要样本级选择、任务相关性远比冗余更重要，或者完整
特征和训练标签已经可以低成本获取时，该方法不太适用。

## 6. 该设计可以支持的主张

只有修正版实验验证成功后，论文才可以主张：

- 谱预筛选减少了昂贵的第二阶段评估次数；
- rank-$L$ Gram sketch 在预先声明的通信预算扫描中接近 Full-Gram greedy，并得到非零排序证书；
- 两阶段选择器能够以更低计算成本接近穷举式任务感知选择；
- 置信度诊断可以识别不应信任紧凑摘要的情况。

论文不应主张：

- 四个摘要量能够对合并有效秩给出普适保证；
- 谱多样性能够单独决定下游准确率；
- 在没有实际使用所选数据进行训练的实验时，声称获得端到端训练收益；
- 在没有形式化或经验隐私泄漏分析时，声称具有隐私保护能力。
- 把 exact-score greedy 称为组合最优或上界。

截至 2026-09-09，`corrected_v3` 的四编码器 rank-$L$ 扫描表明 2,880 个选中 prefix 区间全部
覆盖精确分数，但 $L\leq50$ 的 7,200 个 greedy 步骤没有一个得到认证，DPP 总体上至少同样有竞争力。
单独的 $L=20$ 精确参照在 288 个配置上得到 288/288 区间覆盖、0/720 证书和 0/288 Full-Gram
有序 prefix 完全一致；rank-$L$/DPP/Full-Gram greedy 相对 rank-only 的平均增益分别为
3.65%/5.37%/5.81%。
自然池 Stage-2 也未通过验收：28 个 encoder-target 单元中，验证 oracle 的来源监督 adapter 相对
冻结 identity 平均低 0.0670，target-cluster 95% CI 为 $[-0.0977,-0.0440]$；所有确定性短名单方法
在 28 个单元上均不优于 identity。故当前证据只支持负面审计结论，不能使用端到端收益或安全预筛选
主张。LoRA 与多池混合实验按预注册停止规则不再启动。

## 7. 主要研究问题

1. 在保持最终效用不变时，谱预筛选能够节省多少第二阶段计算？
2. 在自然冗余而不只是人工 clone attack 中，alignment 信息是否优于仅使用秩的摘要？
3. 多大的候选短名单能够以较高概率保留任务感知意义下的最优候选？
4. 预测器诊断在什么条件下能够可靠识别摘要模型失效？
5. 传输更丰富的摘要能够获得多少准确率收益，其通信和隐私成本是多少？
