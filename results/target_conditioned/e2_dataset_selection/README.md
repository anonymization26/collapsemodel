# E2 真实冻结表示实验

> 状态：数据流水线实现中，尚无可报告的真实数据方法结果。

当前已定义官方发布包收据、逐样本内容 manifest、无标签哈希划分和冻结特征缓存的
完整性协议。PACS 与 Office-Home 的真实发布包、manifest 和 ResNet-50/DINOv2 缓存
必须全部通过校验后，才会执行目标条件选择和经典基线比较。

旧版单域 DomainNet 特征缺少本协议要求的完整多域任务、统一预处理和逐样本 ID，
不计入 E2 证据。数据准备阶段不构成 H2 通过，也不产生论文性能主张。

协议与命令见 `docs/E2_DATA_PIPELINE.md`。
