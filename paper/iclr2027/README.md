# ICLR 2027 工作初稿

题目：**Feature Geometry for Data-Pool Screening: Utility, Stability, and Limits**

本版基于 `65a6435` 已提交实验基线重新组织论文。它是供作者审阅的完整工作初稿，尚未提交会议。
2026-09-26 完整算法前置：中英文第3.1节新增 Algorithm 1，集中给出输入、特征与Gram缓存、
目标二阶矩、A-opt评分、短名单、监督拟合、验证选择及输出，明确标签访问阶段和字典序同分规则。
引言引用该算法；第3节按完整流程、评分原理、对照方法、误差分解与成本组织。
有效秩的定义、数值例子与加权Gram公式移入附录A，主文保留其对照定位；MMD不混入主算法步骤。
核对20个合成候选的迹公式与现有特征分解评分、四档短名单及验证同分选择，均一致。
两版原有13个展示公式内容未变；2,331个受保护的代码、结果、测试、参考文献、模板、图表、
生成材料及摘要文件哈希未变，评价协议、实验结果、讨论与结论正文也未变。
两版重编译、自动核验与修改页面视觉检查通过；英文正文第9页结束、共21页，中文正文第8页结束、共18页。
本轮未运行新实验。独立匿名英文副本与ZIP已同步：95项测试全部通过，6组统计分析的19份CSV
与原记录逐字节一致，1,162条归档记录未变。匿名扫描通过；包内无Git历史、私有提交标识、
中文论文或macOS杂项文件，共130个文件，ZIP为43,810,798字节。

2026-09-26 概念澄清：同步修改中英文摘要、引言、方法与结论中的相关表述，明确不确定性指
预测系数的不确定性，目标方向能量不等同于标签重要性。形状/背景仅用作特征方向的直观示例。
将“验证误差”改为验证选择造成的质量差距，说明该差距不等于验证集损失，且遗漏/选择分解
不依赖共享线性模型。英文摘要193词，Markdown与投稿字段同步；正文以替换重复解释为主。
公式、证明、实验协议、实验结果与讨论不变，2,305个已跟踪代码、结果、测试及参考文献文件未变。
两版重编译、自动核验和修改页面视觉检查通过；英文正文第9页结束、共20页，中文正文第8页结束、共18页。
独立匿名英文副本与ZIP已同步，95项复现测试通过，6组分析的19份CSV与原记录逐字节一致；不带入Git信息或中文稿。
未提交或推送。

2026-09-26 文献内容扩展：根据已下载原文，补充六篇新文献的选择机制及与本文的关系，
相关工作仍分三条线；新增附录D，详细区分表示、选择单位、信息访问及未来公平比较的条件。
明确转导目标方差与 Target A-opt 的联系、SIFT 的条件冗余、SkMM 的梯度草图与矩匹配，
以及 TSDS/TAROT 的传输选择和合成数据协方差理论的适用范围。没有新增已完成实验的主张。
英文正文仍在第9页结束、共20页；中文正文第8页结束、共18页。两版重编译、自动核验和
相关页面视觉检查通过；2,269个已跟踪代码、结果及测试文件、两版数学环境、摘要及图表输入均未改变。
本轮只更新开发仓库中的中英文论文，未刷新独立匿名投稿副本，未提交或推送。

2026-09-26 文献整合：将 `reference/` 中尚未引用的6篇补充文献加入中英文稿，共20篇实际引用。
相关工作按目标感知实验设计、矩匹配与数据选择、几何与计算预算三条线组织；引言和方法补充
转导主动学习引用，明确 Target A-opt 与已有目标方差准则的关系。按已核验记录补全3处作者
中间名及PACS论文页码。未将新增文献写成已完成的实验基线；摘要、数学公式、实验与结论不变。
两版重编译及自动核验通过，英文正文第9页结束、共19页，中文正文第8页结束、共17页。
文献版本与核对记录见 [参考文献核对报告](../../reference/BIBLIOGRAPHY_AUDIT.md)。未提交或推送。

2026-09-26 第二轮按作者确认的定位改写中英文摘要和引言：明确提出几何驱动的两阶段数据池
预筛选框架，以 Target A-opt 为核心评分方法，解释源 Gram、目标二阶矩与标签验证的分工。
将既有目标加权实验设计应用于候选数据池并集，不声称首次提出 A-opt 准则；保留 MMD 的竞争力、
预算分析的探索性属性及读出器相关的成本条件。贡献融入叙述，题目、关键词和引言之后的正文不变。
英文摘要180词，与中文摘要、Markdown、投稿字段和 PDF 同步。两版重编译并通过自动核验及
全部36页渲染复核；英文正文第9页结束、共19页，中文正文第8页结束、共17页。
核对2,269个已跟踪实验代码、结果和测试文件，均未改变；图表统计及实验输入哈希不变。
本轮仅修改表达，未运行新实验、未导入 A-opt/MMD 互补性审计，也未提交或推送。

2026-09-26 在 `edf93b3` 上完成15项主线与实验解释修改：先问较少评价能否得到有竞争力的选择，
再检验实际成本，随后区分候选保留、验证选择和域级/块级信息。两阶段分工、误差分解与实验观测对应，
合成风险和真实分类分别说明作用；压缩支线保留在附录。中英文同步，摘要与全部实验数据不变。
英文正文第9页结束、共19页；中文正文第8页结束、共17页。自动核验及两版36页渲染复核通过，
2,181个已跟踪代码/结果/测试文件和两版数学环境与基线一致；本轮未运行新实验，未提交或推送。
2026-09-24 完成投稿前表达与阅读顺序改写：摘要和引言从重复训练、验证的成本问题切入，
方法先解释概念与作用再给公式，实验先报告最终质量与成本，再解释域组成、保留率及敏感性。
贡献融入引言末段，保留独立局限性小节；已有质量成本表前移主文，召回曲线和读出召回表移至附录。
本轮不运行新实验，不改变方法、数字、判定门槛或科学结论，也不自动提交或推送。
英文正文9页、共19页；中文正文8页、共17页。自动核验与全部36页渲染检查通过。
2026-09-22 在 `da7bb0d` 基础上导入 R5/R6：质量预算、固定参数候选重分块、源域控制，以及
独立串行计时和图像特征成本分项。贡献集中为目标感知几何如何减少监督评价，保留 MMD 作为
有竞争力的简单方法；有效秩压缩为背景，补充转导实验设计的直接文献。当时英文正文9页、
总计19页；中文正文8页、总计17页。自动核验和两版全部页面的渲染检查均通过。
2026-09-19 按作者确认的写作原则重写中英文叙述，贡献融入引言末段，单设局限性小节；
导入 2026-09-17 完成的 R3/R4 多投影种子与逻辑回归结果，保留已有 R1/R2 诊断。
同日第二轮从源数据选择问题补充背景，扩写有效秩的构造和目标加权解释；去除正文与附录的行内短语标题。
英文 Conclusion 自然结束于第 9 页，没有改动字号、页边距或行距。
历史主表和门槛不变，不将设备、种子或读出器重复计为新独立数据集。
新增代码和结果纳入本次版本记录，作者科学核验仍待完成。
旧 NeurIPS 稿完整保存在 [归档目录](../archive/neurips2026/)，不与当前稿混用。
此前目录整理未启动新实验；2026-09-22新增实验不改变已冻结的实验门控。

## 文件

- `main.tex` / `main.pdf`：英文稿入口。
- `main_zh.tex` / `main_zh.pdf`：中文对照入口，正文、证明与限制同步。
- `body_en.tex` / `body_zh.tex`：七节主文，讨论与结论分开。
- 第3.1节的算法框完整描述 Target A-opt 预筛选与验证；两版均位于PDF第3页。
- `appendix_en.tex` / `appendix_zh.tex`：数学推导、实现审计、补充失败诊断及近期数据选择文献的详细讨论。
- `abstract_en.txt` / `abstract_zh.txt`：唯一摘要文本源。修改后运行构建脚本，自动同步 `abstract.md` 与 PDF 输入。
- `submission_metadata.json`：唯一中英文题目和关键词源；不存储作者身份。
- `submission_metadata.md`：自动生成的投稿题目、摘要和关键词，与 PDF 使用同一份文本源。
- `build_assets.py`：从记录的结果生成七张表、四张图、数字宏、摘要及投稿字段。
- `generated/asset_manifest.json`：上述图表的输入 SHA-256 及描述性诊断。
- `references.bib`：本版实际引用；第三方论文作者信息不属于本稿署名。
- `iclr2027-style/`：用户提供的完整正式模板，原样保留。
- `.latexmkrc`：优先加载模板附带的 `fancyhdr.sty`、`natbib.sty`，避免系统包版本改变审稿页眉。

## 构建

在本目录执行：

```bash
python3 build_assets.py
latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex
latexmk -xelatex -interaction=nonstopmode -halt-on-error main_zh.tex
python3 verify_draft.py
```

必须保留 `.latexmkrc` 与完整的 `iclr2027-style/` 目录，并从本目录执行上述 `latexmk` 命令。
两个入口都直接引用模板目录中的 `.sty` 和 `.bst`；已不再使用的同名副本及旧英文 XeLaTeX
中间文件已移入 [历史构建材料](../archive/iclr2027_legacy/)，本目录不再保留冗余样式入口。
不要使用未配置依赖搜索路径的裸 `pdflatex` / `xelatex` 命令：它们不读取 `.latexmkrc`，可能重新加载
系统版 `fancyhdr` 并丢失页眉。必须直接调用引擎时，显式设置 `TEXINPUTS=./iclr2027-style//:`，
保留结尾的冒号以继续搜索 TeX 系统目录。首次迁移到完整模板可给 `latexmk` 加 `-g` 强制重建。
算法框使用 TeX Live 提供的 `algorithm` 和 `algpseudocode`，保持正文字号，不修改正式模板文件。

绘图需要 NumPy、Matplotlib。验证使用标准库、NumPy、Poppler 的 `pdftotext` 和 `pdfinfo`。
英文使用 pdfLaTeX/Times；中文使用 XeLaTeX、TeX Gyre 西文字体与 macOS 的 Songti SC、Heiti SC。
中文 `ctex` 仅负责汉字支持与断行，不接管字号、行距或标题格式；两版正文均沿用正式模板的
10pt 字号、11pt 基线行距、零首行缩进和 5.5 × 9 英寸版心。中文保留图表和参考文献名称的翻译。
其他操作系统需要显式替换为已安装的中文字体，不能假设默认回退能正确显示汉字。
PDF 视觉检查使用 Poppler 渲染，不以成功编译代替检查。

2026-09-13 正式模板修订：修复审稿页眉缺失，取消两版表格的 `\small`，三个声明使用模板示例的
无编号二级标题。英文正文结束于第 8 页（共 13 页），中文正文结束于第 7 页（共 12 页）；
中文页数减少来自恢复模板字号和行距，没有删减论文内容。系统宋体仍发出 OpenType Script 元数据提示，
但没有缺字。作者科学复审和新实验仍待完成。

`verify_draft.py` 在已有摘要、数据与引用检查之外，还核验正式依赖文件的 SHA-256、实际加载路径、
正文及版心参数、US Letter 页面、逐页审稿页眉与页码、匿名标题区、正文图表页限及表格字号命令。
这些检查不能代替 PDF 视觉检查或作者科学核验。

## 论证边界

1. 完整 Gram 可决定谱；边际谱与主角度不足。局部双方向公式有显式相容条件。
2. 目标加权 A-opt 风险属于标准共享贝叶斯线性模型，不是 softmax Brier 的普适定理。
3. PACS/Office-Home 的 H2/H2b 仍失败；事后 oracle 改善空间不能挽救原门控。
4. DomainNet H3 支持限定协议下的等成本预筛选，但 MMD 主设置完全持平，小短名单均值更好。
5. 小 Brier 遗憾不等于高准确率；候选完整跨度小于门槛使遗憾子门控失去区分力。
6. 独立计时支持已有特征条件下的计算节省；图像分项核算支持逻辑回归节省、但岭回归收益反转。
   这不是包含候选构造和缓存写盘的完整端到端实测，不声称通信压缩、全局最优或标签隐私。
7. 新增四种子中，两种读出器的 A-opt Brier 前十名召回均为 98.33%；MMD 为 99.38%/98.96%。
   逻辑回归候选平均相对 Brier 跨度为 14.652%，仍有高召回；同数据补实验属于探索性敏感性分析。
8. 固定参数哈希重分块复用原源图像和目标划分，不是独立数据集。重分块后，域级匹配与MMD的平均最终Brier相近；
   域内块信息在原构造下有用，但重分块后优势缩小，且错误率最优候选保留率可低于随机。

后续证据缺口和作者核验任务见 [改写 checklist](../../docs/ICLR_REWRITE_CHECKLIST.md)。

## 当前投稿信息

[投稿字段](submission_metadata.md)提供英文题目、193 词摘要、六个关键词及中文对照。
摘要以几何预筛选框架和 A-opt 设计为主线，只用50与227两个候选预算说明已有探索性结果，
并保留与 MMD 的比较及不同特征复用条件下的成本差异。
第5.1节报告最终损失预算曲线，第5.2节报告独立计时，第5.3节解释候选保留与验证选择，
第5.4节报告候选构造和源域对照，第5.5节报告投影与读出敏感性，第5.6至5.7节交代此前的
合成研究与PACS/Office-Home直接选择研究。附录保留完整比较、配对差异、失败比例、质量成本图和核算表。
原主实验、R1/R2、R3/R4及R5/R6分别报告。科学审计仍复用模型，但新增计时不跨方法共享拟合结果。
原始图像成本是独立测量分项的加总；随机方法按实际短名单源块并集收费，且不提取目标选择特征。
质量成本图使用三次计时实际选择的质量与时间，不与20次随机排名的科学审计均值混接。

2026-09-24 两版 PDF 已重新编译并通过自动核验；英文正文第9页结束、共19页，
中文正文第8页结束、共17页。题目、关键词、摘要文本与投稿字段一致。
核验脚本独立重算240条原补实验汇总、96条预算曲线、312条构造曲线、60条组内诊断和
32条配对质量成本曲线，并检查计时选择、随机源块成本与缓存批次口径；原门槛不变。
核验还要求英文正文恰好结束于第 9 页，正文与附录均无行内短语标题，并检查有效秩例子和目标方向展开式。
两版全部36页均已渲染复核；英文第9页完整包含Conclusion，图表和中文显示无遮挡或缺字。
本轮核对2,272个实验代码、测试和结果文件的SHA-256，均与改写前一致；两版各13个展示公式、
1个命题和1个证明环境逐字未变，图表的实验输入哈希和统计值未变。作者科学复审仍待完成。
2026-09-22的临时Python3.12环境通过56项相关测试，依赖与服务器一致，解决此前缺少 `fsspec` 的测试限制；
本轮表达改写未重跑这些实验代码测试。
详细实验口径与未完成事项见 [R5/R6报告](../../results/target_conditioned/revision_20260922/README.md)。

根据 2026-09-17 核查的[官方作者指南](https://iclr.cc/Conferences/2027/AuthorGuidelines)，摘要截止为
2026-09-18 23:59 AoE，全文截止为 2026-09-25 23:59 AoE。摘要必须真实反映全文，
不能用占位摘要；摘要截止后不能增删作者。这里只准备字段和文件，没有代为提交或确认作者名单。

## 模板来源

`iclr2027-style/` 中的正式模板与依赖未作任何修改；该模板的发布来源为
[ICLR 2027 官方模板](https://media.iclr.cc/Conferences/ICLR2027/iclr-2027-style-files.zip)，未改版式。
依据 [作者指南](https://iclr.cc/Conferences/2027/AuthorGuidelines)，投稿正文最多 9 页；参考文献、附录及
规定的声明不计入正文页限。本版使用匿名样式，页眉由官方模板生成，不表示已经实际投稿。
已按 [AI 使用政策](https://iclr.cc/Conferences/2027/AIPolicyForAuthors) 写入辅助工作范围，但未代替人类作者
声明“所有 AI 工作已人工核验”；该核验保留为提交前待办。
