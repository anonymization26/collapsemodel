\section{证明与数学主张的适用范围}
\label{app:proofs}
\begin{proposition}[方向分配影响合并结果]
存在行归一化的矩阵对 $(A,B_1)$ 与 $(A,B_2)$，它们具有相同的边际奇异值谱和行空间主角度，但拼接后的有效秩不同。
\end{proposition}
\begin{proof}
令 $A$ 包含四行 $e_1^\top$ 和一行 $e_2^\top$；$B_1$ 包含四行 $e_1^\top$ 和一行 $e_3^\top$；$B_2$ 包含一行 $e_1^\top$ 和四行 $e_3^\top$。三者非零奇异值谱均为 $(2,1)$，两组行空间主角度均为 $(0,\pi/2)$。合并 Gram 谱分别为 $(8,1,1)$ 与 $(5,4,1)$，有效秩约为 $2.6260$ 与 $2.8495$。尽管边际摘要相同，共享方向得到的能量却不同。
\end{proof}

设 $u,v$ 为单位方向，$u^\top v=c\in[0,1]$。在正交基 $u,z$ 中写成 $v=cu+\sqrt{1-c^2}z$，算子 $a^2uu^\top+b^2vv^\top$ 表示为
\begin{equation}
 \begin{pmatrix}
 a^2+b^2c^2 & b^2c\sqrt{1-c^2}\\
 b^2c\sqrt{1-c^2} & b^2(1-c^2)
 \end{pmatrix}.
\end{equation}
其迹为 $a^2+b^2$，行列式为 $a^2b^2(1-c^2)$，因此特征值为
\begin{equation}
 \lambda_{\pm}=\frac{a^2+b^2\pm\sqrt{(a^2-b^2)^2+4a^2b^2c^2}}{2}.
 \label{eq:local}
\end{equation}
当 $c=1$ 时，第二分支为零。若相关子块相互正交，且对两个源 Gram 都是不变子空间，汇集这些特征值对便得到完整谱；未配对方向贡献自身特征值。这一相容条件不可省略，因为主向量不必是奇异向量。

固定 $H_S$ 并采用高斯似然，对先验与似然乘积完成平方可得后验精度 $P_0^{-1}+\sigma^{-2}H_S^\top H_S$ 和均值 $\mu_S=P_S\sigma^{-2}H_S^\top y_S$。对独立目标观测 $y=h^\top\theta+\epsilon$，给定训练数据与 $h$，有
\begin{equation}
 \E[(y-h^\top\mu_S)^2\mid H_S,y_S,h]=\sigma^2+h^\top P_Sh.
\end{equation}
对 $h$ 积分得到式\eqref{eq:risk}。这里假设目标协变量分布不会在该模型之外额外透露关于 $\theta$ 的信息。若源数据具有不同回归函数，或标签为多分类且采用 softmax 读出，则该推导不再等于所评价的损失。证明不约束这种模型误设误差。以有限样本估计替代 $C_T$ 还引入另一类偏差，恒等式也不会自动消除它。

若 $G\succeq\widetilde G\succeq0$，逆矩阵序给出 $(P_0^{-1}+\sigma^{-2}G)^{-1}\preceq(P_0^{-1}+\sigma^{-2}\widetilde G)^{-1}$。与 PSD 目标矩相乘取迹保持该顺序。更一般地，若遗漏草图尾部之和满足 $0\preceq G-\widetilde G\preceq\delta I$，定义 $B=P_0^{-1}+\sigma^{-2}\widetilde G$，则
\begin{equation}
 \tr\!\left(\widehat C_T(B+\sigma^{-2}\delta I)^{-1}\right)
 \leq f_A(G)\leq\tr(\widehat C_TB^{-1}).
 \label{eq:interval}
\end{equation}
各 PSD 尾部的有效谱范数上界之和，是一种可取的 $\delta$。当某候选上界低于所有竞争者下界时，可以认证该候选的代理排序。因此，除了覆盖率，区间宽度也决定草图能否支撑下一次选择。归档扫描使用其文档中的字节计数，评价这类代理比较。

令 $S_Q$ 在 $\cQ_s$ 中最小化 $\ell$。短名单上的一致偏差给出
\begin{equation}
 \ell(\widehat S)\leq v(\widehat S)+\varepsilon
 \leq v(S_Q)+\varepsilon\leq\ell(S_Q)+2\varepsilon.
\end{equation}
减去 $\min_{S\in\cC}\ell(S)$ 即得式\eqref{eq:decomposition}。该代数推导没有给出概率声明。若要获得总体泛化保证，必须结合真实依赖与选择流程另外证明偏差事件。有限测试集上的穷举分数本身不足以提供这种证明。

映射 $\phi(h)=hh^\top$ 采用 Frobenius 内积，满足 $\langle\phi(h),\phi(h')\rangle_F=(h^\top h')^2$。源与目标经验特征均值之间的平方距离即式\eqref{eq:mmd}。这是有偏经验平方 MMD，即 V 统计量，而非去掉对角项的无偏估计。它依赖非中心化二阶矩，无法区分所有具有相同二阶矩的分布。我们将其作为计算简单的同信息对照，而非完备分布差异度量。

\section{实现与审计细节}
\label{app:protocol}
原 DomainNet 实验拟合岭回归读出，并将其分数转换为类别概率。权重与 Brier 损失为
\begin{equation}
 \begin{aligned}
 W_S&=(I+G_S)^{-1}\sum_{i\in S}H_i^\top Y_i,\qquad
 p(x)=\operatorname{softmax}(h(x)^\top W_S),\\
 b(S)&=\frac1{n_T}\sum_t\norm{p(x_t)-e_{y_t}}_2^2.
 \end{aligned}
\end{equation}
$Y_i$ 为固定 100 类词表下的源 one-hot 标签矩阵。不使用截距或温度调参。载入 float32 特征后，线性代数以 float64 计算。通过 Gram 特征分解计算有效秩时，先将负舍入特征值裁剪为零，再取平方根得到奇异值，仅保留 $\sigma_j>\sigma_{\max}\sqrt{8\epsilon_{64}\max(n_S,d,1)}$。代码对零矩阵返回零秩。该阈值针对数值秩，不改变熵定义。共享投影种子为 $20260911$，维度为 $128$；冻结 DomainNet 协议内部没有根据结果选择维度。

Bayesian D-opt 最大化 $\log\det(I+G_S)$。子空间 DPP 取每个块的前 16 个 Gram 特征向量 $U_i$，定义 $K_{ij}=\norm{U_i^\top U_j}_F^2/16$、$K_{ii}=1$，将其数值投影到 PSD 相关矩阵，再最大化 $\log\det(K_{S,S}+10^{-8}I)$。该核描述块子空间，而非单图像特征。域平衡最大化已覆盖域数量，减去各域块数平方和的 $10^{-3}$ 倍。确定性分数按组合标识字典序处理并列。这些是固定对照；该 DPP 构造表现不佳，不是对所有行列式选择方法的结论。

DomainNet 记录图像内容哈希、样本标识、官方划分、角色及类别词表。特征提取前验证归档与划分校验和。角色互斥和重复内容检查处理已物化基准内部的重叠，但不能排除语义近重复或预训练污染。全部筛选方法使用相同源块、投影、目标选择行和候选集合。特征提取忽略标签，基准物化则使用类别元数据，并为后续授权阶段存储标签。

筛选、验证与审计 manifest 绑定配置和上游产物哈希，分阶段加载器执行标签与划分权限。原协议的验证阶段单独评价每种方法或随机重复。选择冻结后，测试审计覆盖每个编码器的全部 $6\times455$ 个组合，穷举 oracle 作为研究参照。补实验的缓存策略如下。

CUDA 重建保留原样本标识、候选成员、预处理和超参数，全部十二个 Brier 前十名集合与原实验一致。逐样本损失均值与重建聚合审计的误差不超过 $1.78\times10^{-15}$。每个目标域对其 2,048 个测试样本执行 2,000 次配对 bootstrap，全部组合、编码器和四种损失共享权重。拟合模型与验证选择固定，平局按组合标识处理。这些重采样衡量对测试样本抽取的敏感性。原 Brier 前十名与 NLL、原始平方损失及分类错误率前十名的平均重叠分别为 $\BrierNLLOverlap\%$、$\BrierSquaredOverlap\%$ 和 $\BrierErrorOverlap\%$。

种子 $20260917$、$20260918$、$20260919$、$20260920$ 生成新的 128 维 Rademacher 投影，保持投影前后行归一化。种子 $20260911$ 用作实现对照，不计入新种子均值。逻辑回归采用 L2 正则化、$C=1$、无截距、LBFGS，容差为 $10^{-6}$，最多迭代 1,000 次。若出现收敛警告则终止任务；实际没有出现警告，最大迭代次数为 24。类别词表固定，源数据中缺失类别的概率设为零。

所有方法保留原筛选规则、候选组合和五种短名单规模，由独立进程依次筛选、拟合验证、测试冻结模型。测试特征读取前，核验源码、配置、来源、模型及选择的哈希。每个读出器对全部组合拟合一次，各方法复用这些拟合结果。这种缓存穷举评价用于比较不同短名单的反事实结果。Brier、NLL 和错误率分别使用自己的验证损失作最终选择。原种子岭回归在 5,460 个重建测试组合上的最大损失差异为 $1.78\times10^{-15}$，全部 1,620 个 Brier 验证选择完全一致。

两个编码器、五个种子、两种读出器合计完成 54,600 次拟合。新种子汇总先在任务内平均随机重复，再在目标域内平均编码器和种子，最后平均六个域。每个读出器包含 48 个编码器--种子--目标域重复测量，但仍是共享相同源池的六个目标域单位。

归档保留历史实验编号用于定位结果，不应将其与论文科学问题混淆。以下路径相对于匿名代码仓库：
\begin{itemize}
\item 共享模型与偏移研究（E1）：\path{results/target_conditioned/e1_synthetic/expanded_v3_controlled_rng/}。
\item PACS/Office-Home 对照（E2）：\path{results/target_conditioned/e2_dataset_selection/method_v1/}。
\item 事后改善空间（E2a）：\path{results/target_conditioned/e2_dataset_selection/oracle_headroom_v1/}。
\item 等成本短名单（E2b）：\path{results/target_conditioned/e2b_fixed_cost_shortlist/domainnet_v1/}。
\item 风险草图扫描（E5）：\path{results/target_conditioned/e5_sketch_certificate/expanded_v3_controlled_rng/}。
\item 重建与测试重采样（R1/R2）：\path{results/target_conditioned/revision_20260916/}。
\item 投影与读出器补实验（R3/R4）：\path{results/target_conditioned/revision_20260917/}。
\end{itemize}
{\raggedright
DomainNet 数据 manifest 前缀为 \texttt{1ba25059df70be4d}，候选集 ID 前缀为 \texttt{545fe86c8c9e8304}，汇总 ID 前缀为 \texttt{2d473cb9eb977e85}；完整 SHA-256 位于产物中。原配置为 \path{code/configs/target_conditioned_e2b/domainnet_v1.json}，实现为 \path{scripts/run_target_conditioned_e2b_shortlist.py}。补实验采用 \path{code/configs/target_conditioned_e2b/readout_projection_v1.json} 和 \path{scripts/run_e2b_readout_projection.py}，汇总表位于 R3/R4 目录内的 \path{revision_results/e2b_readout_projection_v1/summary/}。论文的 \path{build_assets.py} 从记录的源文件生成表格、图、数字宏和双语摘要，完整输入哈希记入 \path{generated/asset_manifest.json}。\par}

三份原始特征矩阵未重复存入 Git。元数据记录精确文件哈希、检查点哈希、形状、预处理、提取代码版本和运行环境。重新提取需要原始公开数据及模型权重，不假设不同硬件逐位一致。仓库保留 manifest 和足够的结果表，可在没有这些矩阵时复现报告的汇总结果。

\section{补充结果}
\label{app:diagnostics}
PACS/Office-Home、$K=3$ 时，A-opt 平均 Brier 为 $0.826769$，DPP 为 $0.829162$，MMD 为 $0.828719$。A-opt 减去基线的配对区间分别为 $[-0.007449,0.000731]$ 和 $[-0.003392,-0.000712]$。事后审计覆盖每个任务全部 220 个组合。Oracle 相对 DPP 的改善空间描述性 95\% 区间为 $[0.162,1.617]\%$；仅 PACS photo 在目标单位层面超过原 $2\%$ 门槛。相对父方法的事后测试最优包络，oracle 平均还能改善 $0.087\%$。该包络是基于结果的参照，不是可用于选择的基线。

表\ref{app:readout-table} 给出主规模下完整的 Brier 召回比较。A-opt 和 MMD 平均相对选择遗憾相同：岭回归为 $\RidgeSelectedRegret\%$，逻辑回归为 $\LogisticSelectedRegret\%$。两者全部新种子主设置 Brier 短名单均包含经验 oracle。

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{shortlist_recall.pdf}
\caption{图中展示原 DomainNet 实验的测试 Brier 前十名平均召回率如何随短名单规模变化。虚线为预设召回门槛。全部曲线使用相同候选组合，各规则按其对应权限读取特征。}
\label{fig:recall}
\end{figure}

\begin{table}[t]
\centering
\caption{探索性 DomainNet 补实验评价大小为 $s=227$ 的短名单。测试前十名召回率（\%）先在每个目标域内平均两个编码器和四个新投影种子，再对六个域平均。Ridge 指 ridge-softmax；NLL 和错误率分别使用对应验证损失作最终选择。}
\label{tab:readout}
\input{generated/readout_rows}
\end{table}

\begin{table}[ht]
\centering
\caption{表中报告全部补实验评分规则在 $s=227$ 时的 Brier 前十名召回率（\%）。平均方式与表\ref{tab:readout} 相同，不包括原种子对照。}
\label{app:readout-table}
\input{generated/readout_all_rows}
\end{table}

A-opt 最低分类错误率召回出现在 DINOv2、Quickdraw、逻辑回归、投影种子 $20260918$ 的任务中。测试错误率 oracle 为 $85.9863\%$，验证选择为 $87.6465\%$。虽然前十名召回只有 $\WorstErrorRecall\%$，短名单仍保留 oracle。因此，保留最佳成员与保留前十名回答不同问题，二者也都不同于可用模型的绝对质量。

四编码器、十个构造种子的谱扫描使用六个重叠设置和四种预算，每个草图秩有 960 个配置。秩为 $10,20,50$ 时，相对仅使用秩的对照，Gram 草图在精确合并有效秩上的收益分别为 $2.09,3.81,4.94\%$，DPP 为 $4.90,5.48,5.68\%$。这些是几何目标收益，不是分类准确率。熵连续性界采用\citet{audenaert2007} 这一类型的界；2,880 个已选择前缀区间全部覆盖，但 7,200 个评价的贪心步骤没有一步得到认证。结果来源为 \path{results/two_stage_controlled_rank_sweep_corrected_v3/}。这并不意味着所有低秩草图都必然失败。

独立 E5 研究将草图载荷与紧凑对称 float64 Gram 比较，而非与更大的稠密存储比较。在覆盖完整网格、平均字节比不超过 $25\%$ 的策略中，与 Full-Gram 贪心的最佳所选集合一致率为 $15.42\%$，由自适应秩上限 2 获得，平均字节比为 $14.91\%$。其成对证书率为 $3.91\%$，完整选择认证率为零。自适应回退能够实现完全所选集合一致，但计入此前传输后，平均成本约为紧凑 Gram 字节的 $160\%$，未达到决策与通信的联合要求。参照是 Full-Gram 贪心，而非穷举全局最小解。

另一项早期实验在 21 个来源上训练源监督 bottleneck adapter，使用四个编码器、五个 adapter 种子审计七个目标。目标训练内验证采用三个分层五折划分，选择冻结后独立读取 held-out 目标测试集。冻结 identity 平均准确率为 $0.83654$，未训练随机 adapter 为 $0.77144$，验证 oracle 源监督 adapter 为 $0.76951$。后者比 identity 低 $0.06703$，目标聚类 95\% 区间为 $[-0.09771,-0.04400]$。相对该 adapter 族的小短名单遗憾因而不代表下游收益。这不是 DomainNet 实验，也不是对所有适应方法的否定。结果位于 \path{results/two_stage_repeated_cv_utility_corrected_v3/}。

\subsection{决策预算重分析}
\label{app:budget}
事后预算审计使用四个新投影种子和两种读出器，不挑选有利种子。对每个任务和终点，穷举验证仅按验证损失选择组合，再将其测试损失作为已冻结短名单选择的配对参照。经验测试 oracle 仍是用于审计的下界参照，与该可部署参照不同。保留全部五个原始预算。

\begin{table}[ht]
\centering
\caption{表中报告逻辑回归的最终 Brier 及超过穷举验证参照 $0.005$ 的比例。先平均随机重复，再在域内平均编码器和种子，最后对六域平均。容忍值属于探索性分析，不是新成功门槛。}
\label{app:budget-table}
\input{generated/budget_rows}
\end{table}

绝对差敏感性网格为：Brier 的 $0.001,0.005,0.01$，NLL 的 $0.01,0.05,0.1$，错误率的 $0.5,1,2$ 个百分点。超差指示量在平均随机重复之前计算；对平均损失再应用阈值会回答不同问题。描述性配对 bootstrap 对六个域均值进行2,000次重采样，不涵盖共享源池引起的依赖、验证重采样或独立数据集。全部终点曲线、配对域差和阈值比例见 \path{results/target_conditioned/revision_20260922/budget_audit/}。脚本 \path{scripts/analyze_e2b_decision_budget.py} 核验已记录损失的哈希，并从验证文件构造穷举参照。

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{headroom_diagnostics.pdf}
\caption{两个面板报告不同候选族的事后诊断。左图展示各 PACS/Office-Home 目标内平均编码器后，oracle 相对 DPP 的 Brier 改善空间。右图展示原 DomainNet 任务的相对 Brier 最好到最坏跨度。原评价门槛保持不变。}
\label{fig:diagnostics}
\end{figure}

\subsection{损失分辨能力与测试样本稳定性}
附录图\ref{fig:diagnostics} 的事后损失跨度诊断为理解原遗憾判据提供了参照。所有候选的相对 Brier 遗憾都低于 $5\%$，因此该门槛无法在本实验中区分筛选是否成功。召回率提供了更有信息量的比较，但也引出另一个问题：测试前十名对测试样本的抽取有多敏感？



CUDA 重建首先复现了原实验的全部 Brier 前十名集合。随后固定拟合模型和验证选择，对测试样本进行配对 bootstrap。重采样与原前十名集合平均仅重叠 $\BootstrapOverlap\%$，但 A-opt 和 MMD 仍分别保留重采样前十名的 $\BootstrapRecall\%$ 和 $\BootstrapMMDRecall\%$。在这些测试样本变化下，较宽短名单比精确排名成员更稳定。

改变指标对“哪些组合算好”的影响更大：原实验 Brier 与分类错误率前十名平均仅重叠 $\BrierErrorOverlap\%$。因此，即使预筛选在若干指标上都有较高平均召回，仍应按预期部署指标评价。重采样协议与其他指标比较见附录\ref{app:protocol}。


\subsection{候选构造与域级对照}
\label{app:construction}
固定挑战使用投影种子 $20260917$、两种编码器和两种读出器。每域原有1536张源图像按照固定种子、域标识与样本标识拼接后的 SHA-256 排序，再分成三个512样本块。成员确定不使用标签、特征值或结果；目标选择集、验证集和测试集保持不变。原构造同时重跑作为控制：65520个验证与测试损失值均与此前对应种子的记录完全一致。重分块不再使用 CLIP 分层标签，但仍以原来选出的源图像集合为条件；它不是独立数据集，也没有新增测试样本。

均值匹配最小化 $\|\bar h_S-\bar h_T\|_2^2$，即线性核 MMD 评分。域级匹配额外使用源域标识，以整个源域的矩替代各块的矩，按候选的域样本比例加权，再最小化与目标矩的 Frobenius 距离平方。原挑战按字典序打破平局。由于域级匹配产生大量结构性平局，额外的事后审计使用20个固定种子，在每个同域组成组内随机排列。它在读取测试损失前冻结全部新验证选择，不重新拟合或改变分类器参数。发布的曲线仍保留原平局协议结果。

\begin{table}[ht]
\centering
\caption{构造挑战使用逻辑回归 Brier。最终损失由规模 $s=50$ 的名单内验证选择得到。后两列是每个非单例同域组成组保留一半候选时，对一个经验最优候选的保留率，不是最终选择准确率。先在目标内平均组成组与编码器，再平均六个目标。域级匹配的最终损失使用20次随机平局处理；其组内项为均匀平局的精确期望。}
\label{app:construction-table}
\input{generated/construction_rows}
\end{table}

每个目标有20个含九候选的组成组和十个含27候选的组成组，排除五个单候选组。组内候选具有完全相同的域重数与总样本数。先平均随机重复，再平均组成组、编码器与目标域。域级评分在组内恒定，因此均匀平局下的最优候选保留期望为 $(20\cdot4/9+10\cdot13/27)/30=45.68\%$。字典序的组内结果仍有记录，但不解释为域级方法的区分能力。

哈希重分块后，A-opt 与 MMD 对逻辑回归分类错误率最优候选的保留率分别为 $41.94\%$、$35.28\%$，随机选择为 $45.75\%$。两者的平均 Brier 遗漏仍低于随机，但优势小于原分块。这些依赖指标的观察描述条件性排序表现，既不构成人群保证，也未从特定分块的其他属性中分离出异质性的因果贡献。

完整曲线和同域组成审计见 \path{results/target_conditioned/revision_20260922/challenge_summary/}，域级随机平局结果见 \path{results/target_conditioned/revision_20260922/domain_tie_audit/}。原始三阶段记录通过哈希绑定候选成员、源特征、配置、上游选择与数值文件。本地汇总核验全部数值哈希与上游签名；服务器在访问测试数据前已检查冻结模型哈希。


\subsection{配对质量与计算成本}
\label{app:cost}
计时研究使用原候选构造与投影种子 $20260917$。每个编码器--目标--读出任务独立执行 A-opt、MMD、随机筛选与穷举验证，重复三次，并用固定种子确定方法顺序。BLAS 使用单线程。各短名单检查点测量一次排序后的累计时间，不跨方法复用拟合模型。岭回归按需缓存已使用块的 float64 Gram 与标签右端项，穷举也使用这一实现。首版 float32 Gram 的岭回归时间保留在归档中，但不进入任何表格。

每次计时的验证损失与选择均与冻结科学审计核对。图\ref{fig:cost} 的质量来自同样三次运行实际选中的模型，Brier、NLL 与错误率分别使用对应验证指标。因此，这里的随机质量坐标平均三次计时排名，而不是主科学审计中的20次排名。先在任务内平均重复，再在目标内平均编码器，最后六目标等权平均；时间中位数作为诊断保留。


\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{quality_cost.pdf}
\caption{最终测试 Brier 分别相对于配对决策时间和图像成本分项核算作图。每条曲线包括预算 $10,25,50,100,227$，叉号为455候选的穷举验证。两行纵轴尺度不同。均值使用两编码器、六目标，以及每方法三次独立执行。原始图像成本不包括候选构造、下载、缓存写盘和测试审计。}
\label{fig:cost}
\end{figure}

内存计时不包括阶段包加载、完整性校验和缓存特征投影。表\ref{app:cache-table} 为两个各含六目标的编码器批次单独核算这些操作。目标感知方法加载选择包与验证包，随机和穷举只需要验证包。共享准备成本在每个编码器批次内只计一次，不折算成虚构的单任务时间。

\begin{table}[ht]
\centering
\caption{磁盘缓存批次成本以秒计，包括输入加载、完整性检查、投影和全部十二个编码器--目标决策。这里是两个六目标批次的总时间，而表\ref{tab:cost} 是单任务均值；选择及损失不变。}
\label{app:cache-table}
\input{generated/cache_batch_rows}
\end{table}

原始图像测量包括模型初始化、图像读取与哈希检查、解码、预处理、推理、CPU 特征传输，以及归一化和投影。各源块独立计时；随机筛选只收取实际前缀中块并集的成本，几何评分与穷举验证则使用全部15个可用源块。所有方法还需要目标验证特征，仅目标感知评分需要目标选择样本。不处理测试图像。测量以固定候选池清单为输入，因此不含基于 CLIP 的候选构造。

服务器采用 Intel Xeon Platinum 8352V CPU 和 NVIDIA vGPU-32GB。特征提取使用 CUDA 12.4、PyTorch 2.5.1、不启用 TF32 的 float32 推理、批大小64，以及四个数据加载进程。读出环境为 NumPy 1.26.4、SciPy 1.16.1 和 scikit-learn 1.7.1。特征分项只测量一遍，不清空操作系统磁盘缓存，并计入块级加载进程启动成本。因此，加总结果描述的是这一实现和复用情形，不是与硬件无关的加速比。

全部32条质量时间曲线记录、三种损失终点、配对差异和384条任务级预算记录见 \path{results/target_conditioned/revision_20260922/cost_summary/}，并附有独立计时记录及逐块特征测量。论文核验脚本从这些记录独立重算配对质量坐标、随机块并集和全部加法总成本。

\section{与近期数据选择方法的联系}
\label{app:related-selection}
近期数据选择方法在选择对象、表示相关性的特征以及筛选可读取的信息上有所不同。这些区别有助于定位本文框架，也决定了后续比较应如何构造。此处讨论提供文献背景；本文实际测量的对照仍以第\ref{sec:protocol}节为准。

转导主动学习区分可获取观测的采样区域与需要预测的目标区域\citep{hubotter2024transductive}。ITL 降低目标熵，VTL 则降低目标预测方差之和。其高斯过程表述说明，一个观测即使不来自目标区域，也能帮助目标预测。方差目标与式\eqref{eq:risk} 的联系最为直接：源几何通过目标协变量处剩余的不确定性发挥作用。本文评价完整、固定的数据池组合，再验证保留的预测器，而不是逐次获取单个观测。

SIFT 将冗余纳入目标导向的数据选择\citep{hubotter2025sift}。它在已选序列的条件下，依据特定提示仍有多少不确定性来选择微调序列。因此，样本是否相关不仅取决于它与提示的距离，还取决于此前已经选择了什么。这为理解合并 Gram 提供了一个有用视角：组合的覆盖同样取决于跨数据池的方向对应关系。SIFT 的实验针对特定提示的语言模型微调；本文保持编码器冻结，监督阶段比较在不同数据池并集上训练的读出器。

SkMM 处理高维微调数据选择中的偏差与方差权衡\citep{dong2024skmm}。梯度草图先寻找近似偏差可控的低维参数子空间，再通过矩匹配控制子空间内的方差。其梯度表示来自模型导数，不应与依赖标签的损失梯度选择混为一谈。SkMM 将所选数据与原始数据集匹配，本文的目标二阶矩则由独立的目标选择样本估计。两者都使用矩阵几何，但本文的固定特征投影不执行 SkMM 的梯度空间构造。

TSDS 通过最优传输目标优化采样概率，在分布对齐与多样性之间进行权衡\citep{liu2024tsds}。其密度感知正则减少概率集中于近重复候选的现象，度量空间可以采用语义嵌入或模型相关表示。TAROT 则使用经过投影、白化和归一化的损失梯度特征构造传输成本\citep{feng2025tarot}；白化先削弱相关梯度方向的主导作用，再匹配所选数据与目标分布。与本文的无标签预筛选不同，其监督损失梯度构造同时使用输入和标签。它们是相关的传输方法对照，但样本级输出需要与本文固定块的候选族对应。冻结嵌入上的 OT 评分可以作为受控改编，却不等于复现 TAROT 的损失梯度方法。

\citet{rezaei2026synthetic} 的协方差匹配分析研究在真实数据上加入合成数据、并进行高维线性回归的预测器。该研究刻画测试误差，并在特定条件下建立协方差匹配的最优性，使匹配具有超出几何相似性的风险依据。这里需要区分中心化协方差与非中心化二阶矩：式\eqref{eq:mmd} 在冻结特征上比较后者。本文的分类短名单与合成数据增广分析对应不同的训练和选择设置。

与这些方法比较时，需要固定候选族、样本成本、目标数据角色和最终验证规则。额外标签访问应作为单独的信息设置明确报告。特征或梯度构造、传输优化、预筛选与监督评价均应计入成本。这遵循\citet{yin2025} 将选择和训练共同计入预算的原则，使比较围绕给定计算量最终获得的预测器展开，而不只比较筛选分数。
